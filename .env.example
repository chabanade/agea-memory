# ===========================================
# AGEA Memory System - Variables d'environnement
# ===========================================
# Copier ce fichier vers .env et remplir les valeurs
# cp .env.example .env
# ===========================================

# --- LLM Providers ---
# DeepSeek (defaut, ~30x moins cher que Claude)
DEEPSEEK_API_KEY=sk-xxx

# Google Gemini (embeddings Graphiti - GRATUIT via AI Studio)
# Creer sur : https://aistudio.google.com/apikey
GOOGLE_API_KEY=xxx

# Claude (fallback premium - optionnel)
ANTHROPIC_API_KEY=sk-ant-xxx

# Provider par defaut : deepseek, qwen, claude, ollama
LLM_PROVIDER=deepseek

# Embeddings : gemini (defaut, gratuit) ou ollama (local)
EMBEDDING_PROVIDER=gemini

# --- Telegram ---
TELEGRAM_BOT_TOKEN=123456:ABC-xxx
TELEGRAM_ALLOWED_USERS=MEHDI_TELEGRAM_ID

# --- Zep ---
ZEP_SECRET_KEY=change-me-random-string-64-chars

# --- Base de donnees ---
POSTGRES_USER=agea
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=agea_memory
POSTGRES_DSN=postgresql://agea:change-me-strong-password@postgres:5432/agea_memory?sslmode=disable

# --- Neo4j (Graphiti) ---
NEO4J_AUTH=neo4j/change-me-strong-password
NEO4J_PASSWORD=change-me-strong-password
NEO4J_URI=bolt://neo4j:7687

# --- Backup S3 (Scaleway) ---
S3_ENDPOINT=https://s3.fr-par.scw.cloud
S3_BUCKET=agea-backups
S3_ACCESS_KEY=xxx
S3_SECRET_KEY=xxx

# --- Domaine ---
DOMAIN=memory.hexagon-enr.fr
BOT_DOMAIN=agea.hexagon-enr.fr

# --- VPS ---
VPS_IP=148.230.112.42
